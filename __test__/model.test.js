'use strict';

describe(`model.js`, () => {
  test(`LinkedList's find method should return the present node if its value matches the value given as the argument`, () => {
    expect().toBe();
  });
  test(`LinkedList's find method should return an error if it reaches the final node`, () => {
    expect().toThrow();
  });
  test(`LinkedList's find method should run again on the next node if the value has not been found yet and the next node is not null`, () => {
    expect().toBe();
  });
})
